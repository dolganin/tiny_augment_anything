_target_:         tiny_augment.dataset.build_dataloaders

train_root:       ${project_root}/data/fine_tune/train
val_root:         ${project_root}/data/fine_tune/val
    
transforms:    
  _target_:       tiny_augment.augmentations.get_augmentations
  img_size:       ${model.pretrain_img_size}

train_batch_size: 128
val_batch_size:   256
prefetch_factor:  2

sampler_type:     weighted
num_workers:      16
weights_root:     ${project_root}/data/fine_tune/weights.csv