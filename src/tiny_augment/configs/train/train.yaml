epochs:              120
checkpoint_path:     ${hydra:runtime.output_dir}/checkpoints/${model.object.backbone}
config_path:         ${hydra:runtime.output_dir}/config
monitor_metric:      f1_macro
monitor_mode:        max
patience:            10
save_every_n_epochs: 5