# Review 2026-03-24

Сейчас код нельзя считать корректным: фронтенд не проходит обязательную TypeScript-сборку из-за нескольких вызовов `PageFrame` без required-prop `description`, а на бэкенде endpoint переименования LoRA не ограничен текущим датасетом. По количеству исходных файлов структура в целом нормальна для SPA + API, но дерево репозитория заметно засорено закоммиченными артефактами вроде `__pycache__`, что стоит отдельно почистить.

## Full review comments

- [P0] Pass the required `description` prop to `PageFrame` here — `/workspace_0/code/YSDA/ML_spring/tiny_augment_anything/frontend/src/pages/dataset-stats/DatasetStatsPage.tsx:95-95`
  `PageFrame` declares `description` as a required prop, so this JSX does not type-check under the current `tsc -b` build. As written, the frontend build is blocked until this page passes a description (the same issue also appears on other pages using `PageFrame` without that prop).

- [P0] Pass the required `description` prop to `PageFrame` here — `/workspace_0/code/YSDA/ML_spring/tiny_augment_anything/frontend/src/pages/review/ReviewPage.tsx:62-62`
  This `PageFrame` call is missing the required `description` prop from `PageFrameProps`, which makes the frontend fail TypeScript compilation in the default build pipeline. Until it is provided, opening or shipping the frontend from CI/local `npm run build` will fail.

- [P0] Pass the required `description` prop to `PageFrame` here — `/workspace_0/code/YSDA/ML_spring/tiny_augment_anything/frontend/src/pages/metrics/MetricsPage.tsx:164-164`
  `PageFrame` requires both `title` and `description`, so this usage is another unconditional TypeScript error. Because the build script runs `tsc -b` before bundling, this page alone is enough to keep the frontend from building.

- [P2] Restrict LoRA rename requests to the session’s dataset — `/workspace_0/code/YSDA/ML_spring/tiny_augment_anything/backend/app/api/diffusion_lora_handlers.py:135-137`
  This handler trusts any `adapterPath` inside `runtime_dir`, so a client that sends a path from another dataset can rename that other dataset’s LoRA metadata through the current session. The problem shows up whenever a stale/tampered `adapterPath` is submitted, because `save_diffusion_lora_name` does not verify that the file belongs to the dataset resolved from `session_id`.
